import { Modal } from "../modal"
import { Text } from "../text"

export const FormSelect = () => {
  return (
    <Modal title="Dodaj cijenu" size="default">
      <Text>todo</Text>
    </Modal>
  )
}
