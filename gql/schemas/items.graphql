query myListings($input: FilterItems) {
  me {
    items(input: $input) {
      id
      name
      pricePerDay
    }
  }
}

mutation createItem($input: CreateItemInput) {
  createItem(input: $input) {
    id
  }
}
